import { NotFoundException } from "@nestjs/common"
import { Foods } from "src/food/food.entity"


export function  calc_total_summ(items): number{
    let total_summ = 0
    
    for(let i of items){
        
        
    }
    return total_summ
}

